upstream backend {
	server backend:8000;
}

server {
	listen 80;

	location / {
		proxy_pass http://backend;
	}

	location /media/  {
	    autoindex on;
	    alias /backend/.media_root/;
	}

	location /static/  {
	    autoindex on;
	    alias /backend/.static_root/;
	}
}